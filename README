galery_thumbnail	galery		create thumbnails for galery images
galery_date		galery		set galery object dates from METADATA

extend_marked		jobs		extend marked jobs
obj_thumbnail		jobs		create thumbnails for job obj images
unmark_job		jobs		unmarking old jobs

remove_cam_img		login		remove ftp folders for processed images
tasks			login		original size video creation
tasks_r			login		rescale videos to 640x... (aspect ratio)

add_arhivs		video		add Server_Task for "yesterday" video creation
camera_chk		video		check Camera online status & create Camera_online object
rescale			video		create "rescale" Server_Task objects
video_chk		video		check video integrity (TESTS)

count_comments		garden		count blog entries

termo_data_read		smhouse		read  TermoReadings from log file last lines
termo_draw_graph	smhouse		draw termo graphs at 00 & 12

termo_ktc_misc		smhouse		DELETE ALL TermoReadings
termo_ktc_import	smhouse		ktc old data import from log's
sun			smhouse		get sunrise & sunset data

mp_import		mapplot		import data for mapplot from file
